# ╔══════════════════════════════════════════════════════════════╗
# ║  KITTY CONFIG — "PHANTOM PROTOCOL"                           ║
# ║  A hacker aesthetic for those who live in the terminal.      ║
# ╚══════════════════════════════════════════════════════════════╝

# ─── FONT ────────────────────────────────────────────────────
font_family      JetBrains Mono
font_size        13.0
adjust_line_height 5

# ─── MOUSE ───────────────────────────────────────────────────
copy_on_select  clipboard
strip_trailing_spaces smart

# ─── WINDOW ──────────────────────────────────────────────────
hide_window_decorations titlebar-only
background_opacity 0.88
inactive_text_alpha 0.7
confirm_os_window_close 0

# ─── COLOR PALETTE ───────────────────────────────────────────
# theme.conf is a machine-local symlink (next to this file's symlink in
# ~/.config/kitty) pointing at one of the theme-<name>.conf palettes.
# Switch with the `theme` zsh command; defaults to hacker if missing.
include theme.conf

tab_bar_style           powerline
tab_powerline_style     slanted

# ─── MACOS ───────────────────────────────────────────────────
macos_option_as_alt yes

# ─── KEYBINDS ────────────────────────────────────────────────
map ctrl+shift+h neighboring_window left
map ctrl+shift+j neighboring_window bottom
map ctrl+shift+k neighboring_window top
map ctrl+shift+l neighboring_window right

map ctrl+shift+u scroll_page_up
map ctrl+shift+d scroll_page_down
map ctrl+shift+g scroll_home
map ctrl+shift+e scroll_end

map ctrl+shift+n scroll_to_prompt 1
map ctrl+shift+p scroll_to_prompt -1

map ctrl+shift+v launch --location=vsplit --cwd=current
map ctrl+shift+s launch --location=hsplit --cwd=current
map ctrl+shift+w close_window
map ctrl+shift+z toggle_layout stack
map ctrl+shift+equal resize_window reset

map ctrl+shift+left  resize_window narrower 2
map ctrl+shift+right resize_window wider 2
map ctrl+shift+up    resize_window taller 2
map ctrl+shift+down  resize_window shorter 2

map cmd+t new_tab_with_cwd
map cmd+1 goto_tab 1
map cmd+2 goto_tab 2
map cmd+3 goto_tab 3
map cmd+4 goto_tab 4
map cmd+5 goto_tab 5
map ctrl+shift+] next_tab
map ctrl+shift+[ previous_tab

# ─── LAYOUT ──────────────────────────────────────────────────
enabled_layouts splits,stack

# ─── PERFORMANCE ─────────────────────────────────────────────
repaint_delay 6
input_delay 1


# ─── REMOTE CONTROL ──────────────────────────────────────────
# one socket per instance so `theme` (zsh) can push colors via
# `kitten @ set-colors` instead of a SIGUSR1 config reload, which
# would reset interactive font-size changes
allow_remote_control socket-only
listen_on unix:/tmp/kitty-{kitty_pid}

# ─── SHELL / SCROLLBACK ──────────────────────────────────────
shell_integration enabled
scrollback_lines 10000

# ─── REMOTE VM ───────────────────────────────────────────────
map cmd+shift+d launch --type=tab --tab-title=vm kitten ssh cam-homepod -t "tmux new -As main"

# ─── CLIPBOARD ───────────────────────────────────────────────
# Silence the "program wants to read clipboard" prompt.
clipboard_control write-clipboard write-primary read-clipboard read-primary no-append
